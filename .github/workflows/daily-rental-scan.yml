name: Daily rental scan

on:
  workflow_dispatch:
    inputs:
      scan_mode:
        description: "Use test for a small sample, full for the uncapped scanner."
        required: true
        default: "test"
        type: choice
        options:
          - test
          - full
  schedule:
    # GitHub schedules use UTC and can be delayed or occasionally skipped.
    # Run hourly through the afternoon; the Python script records last_scan_date
    # and sends at most once per London day after 12:00.
    - cron: "0 11-18 * * *"

permissions:
  contents: write

concurrency:
  group: daily-rental-scan
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Playwright
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install playwright
          python3 -m pip install curl_cffi
          python3 -m playwright install chromium

      - name: Run rental scan
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          BRAVE_SEARCH_API_KEY: ${{ secrets.BRAVE_SEARCH_API_KEY }}
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          SCAN_BACKEND: playwright
          PLAYWRIGHT_MAX_PAGES_PER_PORTAL_STATION: 12
          SCANNER_DETAIL_BLACKLIST_CHECK_LIMIT: 150
          PLAYWRIGHT_NAV_TIMEOUT_MS: 12000
          PLAYWRIGHT_SEARCH_NETWORKIDLE_TIMEOUT_MS: 1200
          PLAYWRIGHT_DETAIL_NETWORKIDLE_TIMEOUT_MS: 0
          PLAYWRIGHT_DETAIL_SETTLE_MS: 150
          PLAYWRIGHT_DETAIL_TEXT_TIMEOUT_MS: 2500
          FAST_DETAIL_FETCH_TIMEOUT_SECONDS: 6
          PLAYWRIGHT_STOP_AFTER_STALE_PAGES: 2
          PLAYWRIGHT_SEARCH_PAUSE_MIN_MS: 300
          PLAYWRIGHT_SEARCH_PAUSE_MAX_MS: 900
          PLAYWRIGHT_DETAIL_PAUSE_MIN_MS: 100
          PLAYWRIGHT_DETAIL_PAUSE_MAX_MS: 300
          PLAYWRIGHT_BLOCK_RETRY_PAUSE_MS: 8000
          PLAYWRIGHT_SEARCH_RETRIES: 1
          PLAYWRIGHT_BLOCK_HEAVY_RESOURCES: true
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ inputs.scan_mode }}" = "full" ]; then
              python3 telegram_bot.py --scan-now
            else
              python3 telegram_bot.py --test-scan-now
            fi
          else
            python3 telegram_bot.py --daily-scan
          fi

      - name: Save sent-listing memory and exports
        run: |
          if [ -n "$(git status --porcelain -- scanner_state.json exports)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add scanner_state.json
            if [ -d exports ]; then
              git add exports
            fi
            git commit -m "Update rental scan state and exports"
            git pull --rebase --autostash
            git push
          fi
